__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
dist/
build/
*.egg-info/

# Compiled native-library artifacts are build output, not source.
native/haven-bt/build/
# WinUI/.NET desktop build output is generated locally and must not be committed.
native/Haven.Desktop/bin/
native/Haven.Desktop/obj/
*.so
*.dll
*.dylib
.env
.env.*
!.env.example

# Local household data must never be committed.
data/
receipts/
traces/

# Recorded audio from scripts/smoke_mic_loopback.py: real mic input, never a repo artifact.
scripts/*.wav

# Per-run output of scripts/verify.py -- a local verification record, not a repo artifact.
verification.json
TASKS.md
