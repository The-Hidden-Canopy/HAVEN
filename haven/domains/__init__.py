"""Life domains beyond the governed home loop (spec page 17 placement).

Each domain owns its records and stores here; application services in
`haven.application` host them; HTTP/IPC adapters stay thin.
"""
