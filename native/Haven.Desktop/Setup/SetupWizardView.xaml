<UserControl
    x:Class="Haven.Desktop.Setup.SetupWizardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid MaxWidth="880" Margin="48,40" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Set up HAVEN" FontSize="28" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock x:Name="StepLabel" Grid.Column="1" Text="STEP 1 OF 7" Opacity="0.6" VerticalAlignment="Center" />
            </Grid>

            <StackPanel x:Name="StepIndicator" Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,14,0,0" />

            <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="10" Margin="0,14,0,0">
                <TextBlock x:Name="StepTitle" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
                <Border x:Name="OptionalBadge" Visibility="Collapsed" VerticalAlignment="Center"
                        BorderThickness="1" CornerRadius="4" Padding="6,2"
                        BorderBrush="{ThemeResource AccentTextFillColorPrimaryBrush}">
                    <TextBlock Text="OPTIONAL" FontSize="11" Opacity="0.8" />
                </Border>
            </StackPanel>

            <TextBlock x:Name="ConfigWarning" Grid.Row="2" Margin="0,34,0,0" Visibility="Collapsed"
                       TextWrapping="Wrap" Foreground="{ThemeResource SystemFillColorCautionBrush}" />

            <ScrollViewer Grid.Row="3" Margin="0,18,0,0">
                <StackPanel x:Name="StepContent" Spacing="12" MaxWidth="760" />
            </ScrollViewer>

            <Grid Grid.Row="4" Margin="0,18,0,0" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="ErrorText" TextWrapping="Wrap" VerticalAlignment="Center"
                           Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                <ProgressRing x:Name="BusyRing" Grid.Column="1" Width="22" Height="22" IsActive="False" />
                <Button x:Name="BackButton" Grid.Column="1" Content="Back" Click="OnBackClicked" Visibility="Collapsed" />
                <Button x:Name="NextButton" Grid.Column="2" Content="Next" Click="OnNextClicked"
                        Style="{StaticResource AccentButtonStyle}" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>
