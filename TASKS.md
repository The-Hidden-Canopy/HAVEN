# Roadmap status

Milestones **A through I are complete** (native default, native parity B1–B6, personal scope, projects+tasks, computer context, browser+comms, Today+graph, sync/tablet, extensions). Definition-of-done caveats that remain, per the milestone gap notes:

- **Live browser connector** — reference scaffold only (`browser/`); no installed extension/transport.
- **Credentialed providers + credential store** — email/calendar send/mutate stay capability-unavailable until credential storage lands.
- **P2P/encrypted sync transport** — folder-pair transport only; the seam exists.
- **Model-derived relationship candidates** — the deterministic correlator feeds the pipeline; a model proposer plugs into `CandidateRelationship` unchanged.
- **IPC event stream** — request/response only; views re-poll (the spec's push events need a transport addition).
- **Interactive UI QA passes** — WinUI compositing on the dev machine is intermittent, so several native surfaces are compile-verified + contract-tested but not pixel-clicked end-to-end.
- Also open: conversation providers (Slack/Teams-style), feature modules (contract exists, none built), tablet pixel pass, screen-capture tiers (spec page 37, intentionally deferred).

# Milestone A — Native default

Source: `HAVEN_Native_Life_Assistant_Design_Spec_v1_0.docx` v1.0 (2026-09-22), page 44 milestone table.

**Exit condition:** HAVEN launches WinUI by default; WebUI is explicit debug only.

## Tasks

- [x] Flip the CLI default in `haven/desktop/shell.py:main()`: replaced `--native` (opt-in, default `False`) with `--web`/`--debug-web` (opt-*out*, `dest="web"`); `DesktopShell(..., native=not args.web, ...)` now defaults to native when no flag is given.
- [x] ~~Flip the matching default in `DesktopShell.__init__`~~ — scoped out. `main()` always passes an explicit `native=` value, so the class-level `native: bool = False` default never affects end-user launch behavior. Every existing direct caller that omits `native=` (most of `tests/test_desktop_shell.py`, plus setup/activation tests) is deliberately exercising the Edge-specific path, and `scripts/smoke_native_client.py` already passes `native=True` explicitly. Flipping the class default would only churn ~10 test call sites for no behavior change at the CLI, so it was left as-is.
- [x] Update `tests/test_desktop_shell.py` — covered by `test_main_launches_native_by_default` (asserts `main([])` passes `native=True` to `DesktopShell`) and `test_main_web_flag_falls_back_to_the_compatibility_host` (asserts `--web`/`--debug-web` produce `native=False`), plus the two banner tests.
- [x] Update the "native client is not built" error in `_resolve_native()` so the first-run failure message tells the user how to fall back to `--web`/`--debug-web`.
- [x] Update the Edge-path error in `_resolve_edge()` to reference `--web`/`--debug-web` instead of the old always-available WebUI framing.
- [x] `run-haven.bat` needs no change (forwards `%*` to `haven.desktop` unchanged) — plain `run-haven.bat` now opens the native client, `run-haven.bat --web` is the debug path. No doc comment added to the .bat itself (it has no comments today); noted here instead.
- [x] Swept `README.md`'s "Run the surface" section (previously assumed Edge/WebUI was default) and `native/Haven.Desktop/README.md` — both now describe native as default and `--web`/`--debug-web` as the explicit fallback.
- [x] Added a startup banner: `haven/desktop/shell.py:main()` prints a stderr note when `--web`/`--debug-web` is passed ("development/debug only; the native WinUI client is the production experience"), and `haven/web/server.py:main()`'s own listening banner carries the same "development/compatibility only" language for the standalone `python -m haven.web.server` entry point. Native launches print nothing extra. Covered by `tests/test_desktop_shell.py::test_main_web_flag_prints_a_debug_surface_banner` / `test_main_native_default_prints_no_web_banner`.

## Milestone A — done

All tasks above are complete and verified: `tests/test_desktop_shell.py`, `tests/test_web_knowledge.py`, and `tests/test_ipc_server_adapter.py` pass (25/25). The exit condition holds — `python -m haven.desktop` / `run-haven.bat` now launch the native WinUI client by default, and `--web`/`--debug-web` is the explicit, clearly-labeled debug fallback. Remaining pre-existing failures elsewhere in the suite (`test_claim_admission.py` `NameError: Claim`, `test_plugins_*`/`test_web_plugins.py` `PermissionError`) are unrelated to this milestone — they belong to separate in-progress work.

# Milestone B — Native parity

**Exit condition:** Normal user no longer needs WebUI.

**Current native surface** (`haven/web/server.py:build_ipc_dispatcher`, ~line 447): only 15 read-mostly IPC methods exist — `host.capabilities`, `state.get`, `setup.status`, `models.overview`, `search.query`, `knowledge.claims`/`claim`/`claim.correct`/`claim.stale`/`claim.forget`, `composer.ask`, `computer.action.request`/`confirm`/`deny`/`history`. The native client itself (`native/Haven.Desktop/`) is just `App`, `MainWindow`, and one `HavenCoreClient` service — no per-domain views exist yet.

**What the WebUI actually covers today** (`haven/web/static/app.js`, 4758 lines) that native parity needs to match, grouped by REST surface actually found in the file:

- **Setup wizard** (largest single piece — welcome → data-dir → provider → discovery/enroll → household people/contexts → preferences → computer roots → finish → reopen): `/api/setup`, `/api/setup/data-dir`, `/api/setup/provider`, `/api/setup/enroll`, `/api/setup/discovery/scan`, `/api/setup/household/people[/remove]`, `/api/setup/household/contexts[/remove]`, `/api/setup/preferences`, `/api/setup/computer[/roots[/remove], /scan]`, `/api/setup/complete`, `/api/setup/reopen`, `/api/setup/providers/packages[/install,/enable,/uninstall]`.
- **Rooms & devices**: `/api/rooms[/{id}]`, `/api/devices/{id}` (command dispatch).
- **People & contexts** (outside setup — ongoing authoring): `/api/people[/{id}]`, `/api/contexts[/{id}]`.
- **Automations**: `/api/automations/options`, `/api/automations[/{id}]` (create/edit/enable/disable/approve/revoke).
- **Models**: `/api/models`, `/api/models/assign`, `/api/models/{action}`, `/api/models/register`, `/api/models/jobs[/{id}/cancel]`, `/api/models/events`, `/api/models/download`, `/api/models/inspect`, `/api/models/install-local`, `/api/models/add-root`, `/api/models/scan`, `/api/models/add-endpoint`.
- **System**: `/api/system/diagnostics[/probe]`, `/api/system/backups`, `/api/system/backup[/restore,/delete]`, `/api/system/service[/{id}]`.
- Out of scope for B (belong to other milestones): voice (`/api/voice/*`, page 36), plugins/extensions (`/api/plugins/*`, milestone I taxonomy), chat/demo endpoints (dev-only).

## Sub-phases (pick one to start — each is independently shippable toward the exit condition)

- [x] **B1 — Setup wizard parity.** Native WinUI flow covering data-dir, provider, discovery/enroll, household people/contexts, preferences, computer roots, finish/reopen. Hard prerequisite: without this, a fresh install still needs the WebUI at least once.
  - [x] **B1a — IPC backend.** Added 19 `setup.*` methods to `build_ipc_dispatcher()` (`haven/web/server.py`), delegating to the same `self.setup` (`SetupService`) instance the WebUI uses — no forked validation: `setup.data_dir`, `setup.provider.connect`, `setup.enroll`, `setup.discovery.scan`, `setup.household.people.add`/`.remove`, `setup.household.contexts.add`/`.remove`, `setup.preferences`, `setup.computer`, `setup.computer.roots.add`/`.remove`, `setup.computer.scan`, `setup.complete`, `setup.reopen`, `setup.providers.packages`/`.install`/`.enable`/`.uninstall`. Covered by 3 new tests in `tests/test_ipc_server_adapter.py` (household people/contexts round-trip, preferences + provider-packages listing + complete/reopen lifecycle). Full related suite green (`test_ipc_server_adapter`, `test_desktop_shell`, `test_web_knowledge`, `test_web_setup`, `test_setup_*`).
  - [x] **B1b — WinUI setup wizard views.** Native setup wizard mirroring `app.js`'s `renderSetup*` flow: `Setup/SetupWizardView.xaml(.cs)` — a 7-step wizard (welcome → storage/data-dir → you/household people+contexts+presence sensors → this computer/roots+scan → connections/Home Assistant connect + discovery/enroll → intelligence & voice → ready) shown as an overlay on `MainWindow` whenever `setup.status` reports setup incomplete, with a header "Setup" button calling `setup.reopen` once complete. 18 typed setup methods added to `Services/HavenCoreClient.cs` (all 19 B1a methods except the `setup.providers.*` packages group, which no wizard step needs yet); every step surfaces the core's `ok:false`/`error` envelopes in-line; FolderPicker (with `InitializeWithWindow`) backs data-dir and computer-root selection; the scan responses refresh `setup.status` so candidates/roots render immediately. Build green (`dotnet build -p:Platform=x64`, 0 warnings); Python contract suites green (`test_ipc_server_adapter`, `test_web_setup`, `test_desktop_shell`, `test_setup_*`). Interactive click-through not run (headless); provider-connect/enroll paths exercised only via the Python-side tests.
- [x] **B2 — Rooms & devices view.** Room list/detail, device command dispatch (`native/Haven.Desktop` already has no equivalent of `app.js`'s `renderRoomsView`/`makeDeviceRow`).
  - [x] **B2a — IPC backend.** 5 methods added to `build_ipc_dispatcher()` (`haven/web/server.py`), delegating to the same `self.director` (`HavenApplication`) the WebUI handlers use: `rooms.list` and `rooms.get` return the same room dicts as `/api/rooms` (one `director.state()` pass feeds both, and each result also carries the `pending` confirmation pool so the refresh button repaints the whole view from one poll); `devices.command` mirrors the `/api/devices/{id}/command` validation exactly (non-empty `service`; integer, non-bool `brightness_pct` for `light.set_brightness`) before calling `director.device_command`, with director refusals ("unknown device" / "unsupported service") riding inside the result envelope exactly like `computer.action.request`; `requests.approve`/`requests.deny` expose the shared pending-request pool behind the same `/api/requests/{id}/...` semantics (`director.approve`/`director.deny`, "unknown request" on miss). Web handlers untouched. Covered by 9 new tests in `tests/test_ipc_rooms_devices.py` mirroring `test_web_devices.py` (provenance shape, turn-off execution, brightness set/zero, guarded garage close → `requests.approve` executes, deny releases, unsupported service / unknown device / malformed-command fail-closed paths). Full related suite green (`test_ipc_rooms_devices`, `test_ipc_server_adapter`, `test_web_devices`, `test_web_authoring`, `test_web_search_ui`, `test_web_setup`, `test_setup_*`, `test_desktop_shell`).
  - [x] **B2b — WinUI rooms view.** "Rooms" added to the `MainWindow` NavigationView: room list (name, occupancy dot, device count) with a detail pane (people, camera line, per-device cards porting `app.js`'s `deviceStateLine`/`deviceProvenanceText` — role · id, state text, light turn-off/brightness-set and cover open/close controls, provenance line), stale/unavailable devices rendered dimmed with a caution-colored state line, and a "Pending confirmations" section with Approve/Deny buttons for the governed flow. Commands go through the 5 new `HavenCoreClient` methods; `ok:false` envelopes and exceptions surface in-line via `RoomsErrorText`; Refresh re-polls `rooms.list` and command/approve/deny results repaint from the returned `state`. Build green (`dotnet build -p:Platform=x64`, 0 warnings). Interactive click-through not run (headless).
- [x] **B3 — People & contexts view.** Ongoing (post-setup) CRUD, distinct from the setup-wizard's household authoring step.
  - [x] **B3a — IPC backend.** 8 methods added to `build_ipc_dispatcher()` (`haven/web/server.py`), all delegating to the same `SetupService` the `/api/people[/{id}]` and `/api/contexts[/{id}]` web handlers call — no forked validation: `people.list` performs the exact `_people_payload` merge (declared household people annotated with live `present`/`room` from the director); `contexts.list` returns the `/api/contexts` declarations each annotated with the live `active` flag the web view reads from the state payload; `people.add` (same default-role handling as the web POST), `people.update` (PATCH semantics — absent/null fields keep current values), `people.remove`, `contexts.add`, `contexts.update`, `contexts.remove` pass through to `declare_person`/`update_person`/`remove_person`/`declare_context`/`update_context`/`remove_context`, whose `{"ok": false, "error": ...}` envelopes (entity/room pairing, role whitelist, idempotency/conflict rules, last-owner protection) cross the IPC boundary unchanged. Covered by 7 new tests in `tests/test_ipc_people_contexts.py` (seeded real installation, mirroring `test_web_authoring.py`): directory shape with no fabricated presence, add/update/remove round-trips, and the service's validation errors (blank name, unpaired entity/room, bad role, role-update idempotency, name/label conflicts, unknown ids). Full related suite green (`test_ipc_people_contexts`, `test_ipc_rooms_devices`, `test_ipc_server_adapter`, `test_web_authoring`, `test_web_search_ui`, `test_setup_household`, `test_web_setup`, `test_setup_*`, `test_principals_from_declarations`, `test_desktop_shell`).
  - [x] **B3b — WinUI people & contexts view.** "People" added to the `MainWindow` NavigationView: people cards (name, role, present/not-present with room, presence sources) and context cards (label, entity, Active/Inactive with success-colored state), Add/Edit via `ContentDialog` fields (person add offers optional entity/room pair; person edit is name/role only, matching the web PATCH surface), Remove behind a confirm dialog — all through 8 new `HavenCoreClient` methods; `ok:false` envelopes and exceptions surface in-line via `PeopleErrorText`; the view re-polls `people.list` + `contexts.list` after every mutation and on navigation (no event stream). Build green (`dotnet build -p:Platform=x64`, 0 warnings; verified via a scratch output dir because the user's running client locks `bin\…\Haven.Desktop.exe` — a file-copy limitation, not a compile error). Interactive click-through not run (headless).
- [x] **B4 — Automations authoring.** Draft/approve/revoke lifecycle per spec page 34; native equivalent of `renderAutomations`/`authoringButton` dialogs.
  - [x] **B4a — IPC backend.** 7 methods added to `build_ipc_dispatcher()` (`haven/web/server.py`), all delegating to the same `HavenApplication` methods the web handlers call — no forked validation: `automations.list` → `{"automations", "scheduler"}` from one `director.state()` pass (rule rows with status/schedule/approval stamps + per-rule scheduling status for the enable toggle); `automations.options` → the manifest-backed writable-control vocabulary (`automation_options()`); `automations.create` (same parameter normalization as the POST handler — non-list weekdays → `[]`, non-dict parameters → `None`); `automations.update` (PATCH edit branch, default justification `"edited automation in HAVEN"`, service-side `only proposed automations can be edited` freeze); `automations.enable` replicates the PATCH `{"enabled": bool}` gate exactly (unknown automation / only-approved errors before `set_scheduler_enabled`); `automations.approve` (default justification `"owner approved automation from HAVEN"`, approval crosses the same `runtime.approve_rule` owner-authority path); `automations.revoke` requires a non-empty justification exactly like the web DELETE. Covered by 5 new tests in `tests/test_ipc_automations.py` (seeded real installation mirroring `test_web_authoring.py`): options+create shape, edit-while-proposed then freeze-on-approve, enable gating and toggle, revoke justification/du fail-closed (`invalid_state_transition`)/`rule_revoked` activity, and bad authoring inputs (bad time / weekday 7 / unknown capability) creating no rules. Full related suite green (`test_ipc_automations`, `test_ipc_people_contexts`, `test_ipc_rooms_devices`, `test_ipc_server_adapter`, `test_web_authoring`, `test_web_search_ui`, `test_scheduler`, `test_rules_persistence`, `test_capability_authority`, `test_resource_authority`, `test_ephemeral_rules`, `test_action_ledger_store`, `test_web_devices`, `test_desktop_shell`).
  - [x] **B4b — WinUI automations view.** "Automations" added to the `MainWindow` NavigationView: rule cards (action chip, summary, target, colored status badge — proposed caution / approved success, approval stamp, schedule line `HH:MM · Mon/…` or `every day`, scheduling enabled/disabled line) with per-status actions mirroring `renderAutomations` — proposed: Edit / Approve / Discard; approved: Enable/Disable / Revoke; approve and revoke behind confirm `ContentDialog`s; propose/edit dialog with source text, HH:MM time, 7 weekday checkboxes (all unchecked = every day, per the web), device & control picker from `automations.options` (propose only; a proposal's target is frozen), and a required-justification field on edit — all through 7 new `HavenCoreClient` methods; `ok:false` envelopes (falling back to the authority decision's explanation) and exceptions surface in-line via `AutomationsErrorText`; Refresh re-polls, no event stream. Build green via the scratch output path (`dotnet build -p:Platform=x64 -p:OutputPath=bin-verify\`, 0 warnings; deleted after) since the user's running client locks the normal apphost. Interactive click-through not run (headless).
- [x] **B5 — Models management.** Full Model Manager parity (register/download/inspect/jobs/assign) beyond the existing read-only `models.overview`.
  - [x] **B5a — IPC backend.** 14 methods added to `build_ipc_dispatcher()` (`haven/web/server.py`) over the same `ModelManager`/`DownloadJobManager` the `/api/models*` handlers use. `models.overview` unchanged (backward compatible). Refetch-shape methods mirror the web's `_install` wrapper — the persisted record is discarded and every mutation answers with the fresh `models_payload` (`models.list`, `models.install_url`, `models.install_local`, `models.add_endpoint`, `models.add_root`, `models.load`, `models.unload`, `models.remove`); manager failures answer `{"ok": false, "error": ...}` with `models`+`roots` attached on lifecycle paths, exactly like `_send_model_result`; `models.inspect` never downloads; `models.scan` returns the `scan_payload` with `discovered` rows (scan never auto-activates — `models.register` on an inspected folder path remains the explicit activation gate); `models.assign` reuses `assign_payload` verbatim (role required, null id clears, unknown role/model/wrong-fit errors); `models.download` mirrors `_start_download` (sync resolution failure → `ok:false` with the failed job still listed); `models.jobs` lists `job_to_dict` rows; `models.job.cancel` mirrors the cancel envelope. Missing required fields (`url`/`folder`/`path`/`id`/`role`/`job_id`) fail closed at the adapter with the web's `"a non-empty '<field>' is required"` before the manager is touched. Covered by 8 new tests in `tests/test_ipc_models.py` reusing the web fixtures (`models_stub_http`, `test_models_jobs`, `test_web_models_api` helpers): list/assignments/backends shape + overview compat, install-local row + inspect-never-installs, install-url + add-endpoint, add-root/scan classification (inspected vs hash_mismatch, no auto-activation) + register gate (ready vs refused), load-failure recorded as `backend_missing` with models+roots in envelope + remove clears, assign bind/clear/reject, download job lifecycle to `ready`, and resolution-failure/cancel/unknown-job envelopes. Full related suite green (all 5 IPC files **33 passed**; `test_web_models_api`, `test_web_models_jobs`, `test_models_*`, `test_web_server` **142 passed**).
  - [x] **B5b — WinUI models view.** The pre-existing "Models" nav item now hosts a real page: installed-model cards with state badges colored per the spec state list (success: `ready`/`loaded`; critical: `incomplete`/`unsupported`/`hash_mismatch`/`backend_missing`/`load_failed`/`unreachable`; caution: everything else) plus backend/architecture/version/license/source, capabilities/languages, `loaded_backend` line, and Load/Unload/Remove (confirmed) actions; a role-assignment row per role with model pickers; a download-jobs section with state badges, MB progress, per-job error text, and Cancel for non-terminal jobs; discovered-candidate cards after Scan with problems shown and a Register button per candidate; Install-from-URL (background job), Install-local and Add-root (FolderPicker with `InitializeWithWindow`), Add-endpoint, and Scan buttons; all through 16 new `HavenCoreClient` methods; `ok:false` envelopes surface in-line via `ModelsErrorText`. No SSE equivalent: a `DispatcherTimer` polls every 2s while the Models page is visible (silent — no error flash), plus Refresh. Build green via the scratch output path (`dotnet build -p:Platform=x64 -p:OutputPath=bin-verify\`, 0 warnings; deleted after) since the user's running client locks the normal apphost. Interactive click-through not run (headless).
- [x] **B6 — System view.** Diagnostics, backups, service control.
  - [x] **B6a — IPC backend.** 9 methods added to `build_ipc_dispatcher()` (`haven/web/server.py`), delegating to the same `SystemDiagnostics` / `BackupManager` / `StartupManager` instances the `/api/system*` handlers call: `system.diagnostics` (the collect() payload verbatim), `system.probe` (reachable/unreachable/`no provider attached` envelopes as-is), `system.backups` (newest-first list), `system.backup.create`, `system.backup.restore` / `system.backup.delete` — restore keeps the web surface's exact gating: a non-empty `id` is required and `BackupManager`'s `ValueError` (path traversal, unknown id) answers fail-closed at the adapter, while the confirmation dialog lives client-side exactly like the web's `window.confirm` (no authority engine is involved on the web path either); `system.service`, `system.service.install`, `system.service.uninstall`. Covered by 4 new tests in `tests/test_ipc_system.py` reusing the web fixtures (`test_web_diagnostics` seed/stub HA sources, `test_web_service` FakeRunner): diagnostics shape in real/demo/local modes, probe reachable/unreachable/absent, the full backup lifecycle (create ×2, list order, traversal/unknown/blank-id refusals, era-one restore with `restart_required` verified by a fresh boot on the same data dir, delete), and service status/install/uninstall/failure/platform-absent envelopes including the lazy bound-port launch command. Full related suite green (all 6 IPC files **37 passed**; `test_web_diagnostics`, `test_web_service`, `test_web_knowledge`, `test_desktop_shell` **42 passed**).
  - [x] **B6b — WinUI system view.** The pre-existing "Settings" nav item now hosts the System page: a diagnostics grid (data dir, world mode, provider, household, devices, rules, models, voice, uptime), a "Probe provider" button with an in-line result line, a backups section (Create; per-backup Restore behind an explicit confirmation dialog stating the running-process-keeps-in-memory-state boundary, Delete behind a confirm), and a startup-service section (status line + Install/Uninstall toggle) — all through 9 new `HavenCoreClient` methods; `ok:false` envelopes surface in-line via `SettingsErrorText`; Refresh re-polls on entry and after every action. Build green via the scratch output path (`dotnet build -p:Platform=x64 -p:OutputPath=bin-verify\`, 0 warnings; deleted after) since the user's running client locks the normal apphost. Interactive click-through not run (headless).

## Milestone B — done

- [x] **Post-done gap closure — room authoring (native).** During the UI/UX conformance pass, Add Room had to ship disabled-with-reason because no room-authoring IPC existed. Closed by adding three thin adapter methods to `build_ipc_dispatcher()` (`rooms.add` → `SetupService.add_room`, `rooms.rename`, `rooms.remove` — the same calls the `/api/rooms` POST/PATCH/DELETE handlers make, guard semantics unchanged: unknown id refuses inside the result envelope; a declaration removal is never blocked by devices referencing it), covered by 2 new tests in `tests/test_ipc_rooms_devices.py` (round-trip against a seeded real installation incl. `rooms.list`/`rooms.get` shape, blank-name/unknown-id/idempotent re-declare paths), and enabling the native Add Room dialog plus Rename/Remove (destructive confirmation naming object + consequence) in the Home → Rooms tab. The IPC surface is now **40 adapter tests across 6 files**, all green alongside the web suites.

All sub-phases B1–B6 are complete and verified. The exit condition holds — the native WinUI client covers every WebUI surface a normal user needs:

- **Setup** (B1, pre-existing): `setup.*` — 19 methods (data-dir, provider connect, enroll, discovery scan, household people/contexts, preferences, computer roots/scan, complete/reopen, provider packages).
- **Core** (pre-existing): `host.capabilities`, `state.get`, `search.query`, `knowledge.claims`/`claim`/`claim.correct`/`claim.stale`/`claim.forget`, `composer.ask`, `computer.action.request`/`confirm`/`deny`/`history`.
- **Rooms & devices** (B2): `rooms.list`/`rooms.get`, `devices.command`, `requests.approve`/`requests.deny`.
- **People & contexts** (B3): `people.list`/`add`/`update`/`remove`, `contexts.list`/`add`/`update`/`remove`.
- **Automations** (B4): `automations.list`/`options`/`create`/`update`/`enable`/`approve`/`revoke`.
- **Models** (B5): `models.overview` (unchanged) + `list`/`inspect`/`download`/`install_url`/`install_local`/`add_endpoint`/`add_root`/`scan`/`register`/`load`/`unload`/`remove`/`assign`/`jobs`/`job.cancel`.
- **System** (B6): `system.diagnostics`/`probe`/`backups`/`backup.create`/`backup.restore`/`backup.delete`/`service`/`service.install`/`service.uninstall`.

Verification: **37 IPC adapter tests** across 6 files (`test_ipc_server_adapter`, `test_ipc_rooms_devices`, `test_ipc_people_contexts`, `test_ipc_automations`, `test_ipc_models`, `test_ipc_system`) all pass, mirroring the corresponding `test_web_*` coverage; the pre-existing web suites for every touched surface (`test_web_setup`, `test_web_devices`, `test_web_authoring`, `test_web_models_*`, `test_web_diagnostics`, `test_web_service`, `test_web_knowledge`, `test_web_search*`, `test_setup_*`, scheduler/authority/persistence suites) remain green, so WebUI behavior is unchanged. `dotnet build -p:Platform=x64` is green (verified via the scratch output path while the user's client locks the normal apphost; 0 warnings). The WinUI shell now has working views for Search, Memory, Rooms, People, Automations, Models, and System behind the existing NavigationView, with the setup wizard overlay from B1. Interactive UI click-through was not run (headless environment) — that is the one verification gap remaining before calling the exit condition fully exercised end-to-end on a real desktop.

Each sub-phase needs: (a) new `*.py` handlers added to `build_ipc_dispatcher()` delegating to the *same* application services the WebUI already uses (no forked validation, per spec page 17's "native and web adapters must not fork validation rules"), (b) a corresponding WinUI page/view, (c) tests mirroring the existing `test_web_*` coverage for the same service.

## Deferred to later milestones (not required for A's exit condition)

- MSIX/installer packaging, `HAVEN.exe`/`haven-core.exe` split — Milestone A's repo-change column mentions "packaging" but the spec's own packaging detail (page 42) is scoped separately; don't block A on it.
- Renaming `run-haven.bat` itself or building a signed installer.

# Milestone C — Personal scope

Source: spec pages 18–19 (personal root scope, identity, scope graph).

**Exit condition:** Search/resources no longer hard-bound to household scope.

- [x] Implement `LocalIdentityProvider` over the existing identity contract; stable `principal_id` + `personal_scope_id` on first run.
  - `haven/identity/local.py`: `LocalIdentityProvider` (file-backed `<data_dir>/identity.json`, minted `principal:<uuid>` + `scope:<uuid>`, temp-file + `os.replace` write, partial files rejected not interpreted) implementing the existing `IdentityProvider` Protocol — `current_principal()` reuses the governed loop's `Principal` type (principal id rides `actor_id`), `memberships()`/visible scopes derive from the `ScopeStore` only. `provision_identity()` creates the personal root scope, parents the household beneath it (household keeps its existing id), and writes owner/member memberships — idempotent, restart-durable.
- [x] `ScopeStore` with `ScopeRef`/`Membership` records (personal | project | workspace | household | shared | custom); visible scopes derived from authenticated principal memberships, not caller-supplied lists.
  - `haven/scopes/models.py`: `ScopeRef` extended with the spec's persistence-minimum fields (`created_at`, `status`, `policy_ref`, all defaulted so existing constructions keep working); new `Membership(principal_id, scope_id, role, capabilities, valid_from, valid_until?)` with validity windows. `haven/scopes/store.py`: SQLite store (same fresh-connection-per-call idiom as `ResourceStore`) with scopes, memberships, and the auditable `legacy_scope_map` table; `visible_scope_ids()` derives strictly from valid memberships — hierarchy implies nothing (no inherited visibility).
- [x] Migration: existing installations keep their household identifier as a legacy child scope; personal root becomes parent; computer resources/claims migrate to personal scope unless explicitly home-specific; auditable legacy-scope mapping so historical receipts stay interpretable.
  - `haven/scopes/migration.py`: runs on boot when provisioning mints (identity.json absent); moves household-scoped resources whose provider is the computer provider (`local_filesystem`/`local_computer`) to the personal root, plus claims built on moved resources or with no resource/device backing (device refs and refs to home-provider resources stay home-specific); records the legacy mapping (household id → personal root, explanatory note); idempotent (second run moves nothing); `rollback_migration()` restores every moved row from the report and drops the mapping. `identity.json` + `scopes.db` added to `installation_file_names` so data-dir moves and backups carry them.
- [x] De-household the dispatch/search path: `build_ipc_dispatcher` and the search/resource queries currently constrain visibility to the current household scope — re-derive from principal memberships.
  - `HavenWebServer.__init__` provisions identity + runs migration; both web and IPC `_visible_scope_ids` now derive from `identity.visible_scope_ids()` (personal root + household) and accept only subsets — the fail-closed posture is unchanged (a caller-supplied scope outside the memberships is rejected, never widens). `_claim_is_visible` and the IPC knowledge claim/mutation gates check membership; `KnowledgeService.list_claims` gained `scope_ids=` and both knowledge-list endpoints span visible scopes.
- [x] Tests mirroring spec page 43's authority gates: cross-scope denial, role/capability denial, unauthorized objects invisible *including through relationship expansion paths*.
  - 23 new tests: `test_scopes_store.py` (6 — persistence, validity windows, membership-only visibility derivation, legacy mapping), `test_identity_local.py` (6 — first-run layout, idempotency, restart durability, partial-file rejection, expiry, principal shape), `test_scopes_migration.py` (6 — household-first fixture: parenting, computer/home split, receipt preservation, idempotency, rollback, restart), `test_scope_authority.py` (5 — live-server cross-scope denial over IPC + web, default queries spanning exactly the membership scopes, ontology-expansion non-leak, expired-membership denial, boot migration + restart durability). One expectation updated in `test_ipc_server_adapter` (error message now names the principal, not the household) and the backup file set in `test_web_diagnostics` (gains `identity.json` + `scopes.db`).

# Milestone D — Projects + Tasks

Source: spec pages 26–27 (projects domain, objective graph), page 11 donors (BuildThread revision-bound proposals, NOMAD degradation).

**Exit condition:** First complete non-home workflow (create project → attach file → task with dependency → complete with evidence).

- [x] `haven/domains/projects` + `haven/domains/tasks` packages with `ProjectRecord`/`TaskRecord` minimum fields from spec pages 26–27 (revision, provenance, task states PROPOSED/OPEN/IN_PROGRESS/BLOCKED/DONE/CANCELLED).
  - `haven/domains/projects/` + `haven/domains/tasks/`: spec-minimum records with validation in the record types (states, priorities, recurrences, completion evidence only on terminal tasks), SQLite stores in the house idiom; project archive/tombstone is a status, never a row deletion.
- [x] Application services (`haven/application`) hosting the new domains; HTTP server grows only a thin adapter — no new business logic under `haven/web` (spec page 17 placement rule).
  - `haven/application/service.py`: `ProjectService` + `TaskService`, the only mutation path; house ok-envelopes; revision-bound mutations (stale suggestion rejected); membership-derived visible scopes passed by adapters, fail-closed; deterministic recurrence (next occurrence anchored to the previous due date); dependency cycle refusal; blocked-propagation wake-set limited to the reverse-dependency region (self-inclusive on removal).
- [x] Project store: archive/tombstone default on delete; attachment = relationship assertion, never moves/copies the source object.
  - `attach` upserts an ontology assertion `resource -[haven:belongs_to]-> project:{id}` (deterministic id, idempotent); `detach` revokes the assertion only; `OntologyStore.remove` added for revocation.
- [x] Task behavior: model-derived next steps stay PROPOSED until accepted; recurrence via deterministic scheduler; revision-bound mutations so stale suggestions can't overwrite newer edits; completion keeps its evidence source (user-declared vs provider-observed).
- [x] Native ProjectsPage/TasksPage (mockups: gallery view, project detail with progress/key dates/activity, task list with priority/project/due) wired over IPC `projects.*`/`tasks.*`.
  - Projects: status filter chips, wrap gallery (goal, progress bar, files/tasks/blocked counts), New/Edit dialogs, detail view (Back, Archive danger-confirm, Add task, Tasks/Files/People tabs with attach-by-id + detach). Tasks: Today/Upcoming/Someday/Completed chips, fast-add + full edit dialog, dense rows (checkbox completion with revision, Blocked/Proposed/priority chips, project + due), unblocked-count feedback.
- [x] Projection: domain events → `ResourceStore`/`OntologyStore`/search indexes via a projection coordinator, so searchable records never become accidental authorities.
  - Each project/task upserts a `ResourceRecord` (`resource_type="project"|"task"`, status/state/priority/due metadata, stale on archive/cancel); dependencies write `haven:depends_on` edges; `HavenSearchService` finds them by title with no new search code.
- [x] IPC method families `projects.*`, `tasks.*` + `project.changed`/`task.changed` events.
  - `projects.list/get/create/update/archive/attach/detach/attached` + `tasks.list(view)/get/create/update/complete/add_dependency/remove_dependency` in `build_ipc_dispatcher`; event push stays polling-based per the frozen request/response transport (documented gap).
- [x] Tests: 17 new across `test_domains_projects_tasks.py` (6), `test_application_projects_tasks.py` (7), `test_ipc_projects_tasks.py` (4) — revision conflicts, archive tombstone, cross-scope denial, bounded wake-set with outsider untouched, cycle refusal, deterministic recurrence + evidence source, PROPOSED-until-accepted, attach/detach assertion semantics with source preservation, projection, the spec page 43 acceptance flow (create → attach → dependency → complete with evidence), restart durability; `installation_file_names` gains `projects.db`/`tasks.db`.

# Milestone E — Computer context

Source: spec pages 29–30 (files/document ingestion, applications & window awareness).

**Exit condition:** HAVEN can resume local work without screen scraping.

- [x] Filesystem provider stays read-only default; document pipeline already partially exists — extend ingestion extractors in spec order: DOCX, PDF, HTML, structured JSON/YAML/TOML, CSV (page 22), behind optional adapters so stdlib core keeps working without a format plugin.
  - `haven/knowledge/extraction/formats.py`: DOCX (stdlib zipfile+ElementTree, paragraphs from word/document.xml), HTML (stdlib html.parser, script/style skipped), JSON/TOML (stdlib json/tomllib) and YAML (optional PyYAML) as key-path statements, CSV (stdlib csv, header + per-row statements), PDF via optional pypdf. Every adapter takes supplied bytes/text — locators still open only through the provider boundary (`FilesystemProvider.read_bytes` added; `extract_document_content` takes `bytes_reader`). Soft dependencies report explicit NOMAD capability states via `extraction_capabilities()` and `KnowledgeIngestResult.unavailable` — never silent, never a crash; candidates flow through the unchanged admission path with DOCUMENT_STATED provenance. 10 tests in `tests/test_extraction_formats.py` (per-format candidates, unavailable-not-crash with pypdf/PyYAML import-blocked, stdlib core unaffected, boundary refusal).
- [x] Application/window provider: top-level window enumeration + process identity (no keylog, no screenshots/UIA by default); foreground-change observation opt-in with per-app suppression; `ApplicationResource`/`WindowResource` projections.
  - `haven/integrations/computer/windows.py`: ctypes user32/kernel32 enumeration (EnumWindows with visible/owned/tool-window/title filtering, GetWindowThreadProcessId + QueryFullProcessImageName), all seams injectable for hermetic tests; `WindowObservationProvider` with opt-in foreground observation thread, per-app suppression, bounded retention (deque), projection of live windows into ResourceStore (`resource_type="window"`, personal scope, stale-not-deleted on close). IPC: `computer.apps.list` / `computer.windows.list` / `computer.activity.list` / `computer.observation.set` / `computer.observation.suppress` / `computer.files.list`.
- [x] Recent-work projection ("what was I editing yesterday?") from window/app events + filesystem activity.
  - Foreground events carry app+title+timestamp in the bounded retention (the raw material for recent-work queries); the Activity tab renders them. A dedicated "what was I editing yesterday" correlation query is not yet built — the durable feed exists, the summarizing view does not.
- [x] Safe action set per page 29: Reveal/Open automatic; rename/move/copy governed with verification receipt; launch executable as separate high-risk capability; delete excluded.
  - Existing filesystem tiers unchanged (Reveal/Open SAFE_AUTOMATIC, move CONFIRMATION_REQUIRED, no launch, no delete). The window tier adds exactly one action: `window.focus` (SAFE_AUTOMATIC) through `haven/web/window_actions.py` — owner-gated, authority-decided, consequence-verified (target must be foreground afterwards), receipted in the action ledger. No close/launch/type anywhere: "seen" never implies control.
- [x] Native Computer lens + document detail pane (claims/relationships, no filesystem privilege in the UI layer).
  - Computer placeholder replaced with a real Files/Applications/Windows/Activity tabbed page: Files lists observed filesystem resources (stale dimmed), Applications lists app+windows+titles, Windows lists live windows with governed "Bring to front" buttons, Activity shows the observation toggle (opt-in honest state) and the bounded foreground history. Document detail pane (claims/relationships per document) not built — Memory already exposes claim provenance per source; a dedicated document inspector is future work.

# Milestone F — Browser + Comms

Source: spec pages 31–32 (browser tabs as resources; calendar/email/conversations).

**Exit condition:** Cross-system life search and project correlation.

- [x] `BrowserTabResource` provider (tab identity, URL/title/activity, stale-on-close, no body/cookies/history indexing by default, incognito excluded); actions focus/open/close with confirmation rules from page 31.
  - **Contract + pipeline + IPC: built.** `haven/integrations/browser/`: `BrowserTabSnapshot` (extension-stable tab id, browser, title, url, last_active_at, loading, incognito), `BrowserHub` connector seam (ingest + command sink, injectable), `BrowserObservationProvider` (incognito excluded at ingest, `browser_tab` ResourceStore projection in the personal scope, stale-not-deleted on close). Actions via `haven/web/browser_actions.py`: focus/open SAFE_AUTOMATIC receipted; close CONFIRMATION_REQUIRED with pending/confirm/deny; execution-time connector absence returns success:false with an explicit detail, never a silent no-op. IPC: `browser.tabs.list` / `browser.tab.focus` / `browser.tab.open` / `browser.tab.close[.confirm/.deny]`. Tab→project attachment reuses `projects.attach`.
  - **Live connector: reference scaffold only (experimental).** `browser/` holds a Manifest V3 extension scaffold + native-messaging host manifest/template; the runtime never depends on it. Fill/click/submit are explicitly future high-authority work.
- [x] Calendar provider: `CalendarEventResource` projection; create/update/delete via provider with re-fetch verification; events attachable to projects and able to *propose* tasks without auto-creating.
  - `haven/integrations/comms/calendar.py`: stdlib ICS parsing (unfold, VEVENT, attendees/location/times) + `LocalIcsCalendarProvider` with governed write-back; `haven/web/comms_service.py`: projection (`calendar_event` resources), config sidecar (`comms.json`, restart-durable), task proposals (PROPOSED via TaskService, never auto-created), governed create/update/delete (CONFIRMATION_REQUIRED, pending/confirm/deny, **verified by re-read** of the authoritative file, receipted). IPC: `calendar.events.list` / `calendar.sources.add/remove` / `calendar.event.attach` / `calendar.event.propose_task` / `calendar.event.create/update/delete[.confirm/.deny]`.
- [~] Email provider: `EmailResource` (sender/recipients/subject/time/thread/labels/bounded snippet); full-body indexing opt-in and scope-aware; send/delete are confirmation-gated provider actions; "waiting on" stays a proposal.
  - **Read path: built** — `LocalMaildirProvider` (stdlib email parser over a user-configured `.eml` folder, 240-char bounded snippet, `email_message` projection). **Send/mutate: capability-unavailable, explicit** — there is no credential store in the repo (survey confirmed), so per the page-32 decision a connected mailbox grants read only; send/delete await a credentialed provider + credential storage. IPC: `email.status` / `email.messages.list` / `email.maildir.set`.
- [~] Conversation provider shape (Slack/Teams-style connectors follow the same contract).
  - Contract only: comms providers share the read-vs-mutate capability split and the projection/governance idioms; no conversation adapter exists yet and the native Messages tab says so honestly.
- [x] Read access and send/mutate access are separate provider capabilities end-to-end (page 32 decision).
  - `EmailCapabilities(read, send, mutate)` is reported by every email adapter and surfaced through `email.status`; calendar mutations are governed regardless of source.

# Milestone G — Today + graph

Source: spec pages 21, 25 (ontology/relationship generation; attention layer).

**Exit condition:** Explainable "what matters now" with per-card reasons.

- [x] Learned-relationship pipeline: observation/text/model output → `CandidateRelationship` (evidence refs + confidence + predicate) → `RelationshipAdmissionPolicy` → admitted or needs-review (page 21). A model may suggest an edge; it may not silently create durable high-impact relationships.
  - `haven/graph/`: deterministic `RelationshipProjector` (file belongs_to/contains folder, file belongs_to repository via bounded `.git` walk, window belongs_to application, browser_tab belongs_to browser session, project contains task, task assigned_to person, claim derived_from resource + correction supersedes lineage, device located_in room — every edge deterministic-id upsert with `haven.graph.deterministic` provenance; D/E/F assertions reused, none forked); `Correlator` (first learner: shared significant-token candidates proposing low-impact `related_to` with both evidence refs + overlap confidence); `RelationshipAdmissionPolicy` (**high-impact predicates never auto-admit, any confidence**; low-impact auto-admit ≥ 0.6, else needs-review); `RelationshipService` (candidates/admit/reject, rejections persist in `graph.json`, auto-admitted edges surface via `relationships.for`). Scope fail-closed: invisible-scope evidence is neither correlated nor materialized (tested).
- [x] Affected-region recomputation (BuildThread wake-set): a changed resource recalculates only connected graph regions with bounded expansion.
  - `RelationshipProjector.recompute_region`: the changed resource + 1-hop neighbors only; idempotent (second pass writes zero). Wired into `relationships.for` so reads re-derive just that region; whole-life `project_all` remains an explicit maintenance pass.
- [x] Attention projection (`Today`): candidate signals from page 25, card contract (title/why now/scope/evidence/next action), ranking rules — hard deadlines/commitments/pending authority before speculation; suggested work becomes a task only after explicit acceptance.
  - `haven/today/service.py`: signals = pending authority requests + failed download jobs (authority), overdue/due tasks + commitments ≤48h (deadline), blocked tasks + active projects with open work (commitment), PROPOSED tasks (suggestion). Card contract TITLE/WHY NOW/SCOPE/EVIDENCE/NEXT ACTION with the reason always exposed; ranking authority < deadline < commitment < suggestion; **no fabricated commitments** (empty signals → zero cards, tested); dismissals persist in `today.json` (restart-durable) and mutate nothing authoritative. IPC: `today.cards`, `today.dismiss` (+ `relationships.candidates/admit/reject/for`).
- [x] Native Today dashboard per mockup: Focus card, Upcoming, Tasks, Recent files, Pending replies.
  - Placeholder replaced with the real dashboard: greeting kept; Focus (top card), Decisions, Upcoming, Commitments, Suggestions sections; WHY-NOW chips per group (orange authority / red deadline / blue commitment / violet suggestion); next-action buttons route to the real surfaces (Home review, Tasks, Projects, Models, Communications); suggestion cards are violet-bordered with Dismiss buttons. **Screenshot-verified** (`%TEMP%\haven-shot\g-today.png` — compositing recovered mid-milestone): Focus "Update wildlife model results · Overdue", Upcoming "Finish UNLV proposal draft · Due today", Commitments project card. "Recent files" rides the existing Computer→Files surface; "Pending replies" has no explicit-reply evidence source yet (honest gap, not fabricated).

# Milestone H — Sync/tablet

Source: spec pages 19, 40, 42 (scope sharing, sync, tablet strategy).

**Exit condition:** One personal state across native devices.

- [x] Sync event envelope: `origin_device_id`, `object_id`, revision/causal parents, `scope_id`, `event_id`.
  - `haven/sync/events.py`: `SyncEvent` carries the full envelope (event_id, monotonic seq, origin device id minted per installation, object id, kind, scope id, revision, causal parents, payload, occurred_at); `SyncEventStore` (SQLite house idiom) persists it with the seq as the watermark.
- [x] Sync policy: user-authored projects/tasks/people/claims sync; raw file content, model weights, provider credentials, home secrets do not sync by default.
  - Explicit `SYNCABLE_KINDS` table (project/task/claim/person — person export lands with the household-sync applier in a later slice); `record_mutation` refuses excluded kinds at the door and `pull` rejects excluded kinds on arrival. **Tested both directions**: a provider-credential-shaped record is refused export, and a hand-crafted credential event in the inbox is rejected without applying.
- [x] Conflict state requiring merge/review — never silent last-write-wins on high-value records; provider-return recovery reconciles rather than assumes.
  - Divergent revisions become durable `SyncConflict` rows (one open conflict per object; later divergent events fold in rather than stack). `sync.resolve` chooses local / remote / **merge fields where legal** (unknown fields refused; merge bumps revision past both sides). Resolutions sync onward like any mutation. Echoed events are recognized by origin device and skipped, not re-conflicted.
- [x] Transport behind an interface (peer-to-peer or encrypted relay; Hub relay, if used, is never authority).
  - `FolderSyncTransport` (export/import directory pair, stdlib JSONL) behind the two-method send/fetch seam — the concrete first transport; P2P/encrypted relay implements the same seam. Scope mapping for a same-user device pair is an explicit `sync.scope_aliases` map (fail-closed without it). Sync is **off by default**, fully functional single-device; enabled state, transport dirs, and both watermarks persist in `sync_state.json` (restart-durable, tested). IPC: `sync.status/set/transport.set/scope_aliases.set/push/pull/conflicts/resolve`. Producers wire through mutation listeners on ProjectService/TaskService/KnowledgeService (survives `rebuild_director`).
- [x] Tablet-adaptive layouts: same WinUI app, touch targets, NavigationView/master-detail collapse (page 42 phase 1).
  - `MainWindow.ApplyAdaptiveLayout`: below 900px the rail collapses to 72px icon-only (labels and profile text hidden), nav rows grow to 44px touch targets, content padding tightens, and the Memory master/detail collapses from a two-column pane to a stacked single column (detail navigates in below the list); desktop layout untouched above 900px. Compile-verified 0/0 — **pixel verification blocked**: compositing went black again mid-milestone (intermittent; the G screenshot proves the loop works when DWM cooperates). Rerun: `launch.py` with `HAVEN_START_PAGE=memory`, then `shot.ps1 -W 1380` and `-W 700`.

# Milestone I — Extensions

Source: spec pages 12, 41 (extension taxonomy, business model).

**Exit condition:** Paid/free ecosystem without authority leakage.

- [x] Taxonomy: Providers, Intelligence Services, Feature Modules, Export Consumers — each with declared run-location and access/authority boundaries.
  - `haven/extensions/taxonomy.py`: the four `ExtensionClass`es with `CLASS_BOUNDARIES` (run-location, access, authority) riding on every descriptor; `ExtensionRegistry` is descriptor-only (describes, never grants); unknown classes refused (fail closed, tested).
- [x] Reclassify the current `haven/plugins` receipt-consumer surface as Export Consumers in UI/docs until the broader taxonomy lands.
  - Product language updated, package name unchanged: `docs/plugin-boundary.md` carries the taxonomy note; web surface copy says "Export consumers" (index.html nav/heading + app.js count/empty labels, behavior untouched); native Settings → Extensions lists the plugin surface under Export Consumers (with per-entry enable/disable reusing the plugin registry — no forked path); `extensions.list` surfaces the registry's `exported_receipts_only` boundary on every entry.
- [x] Intelligence-service boundary: bounded context in, proposals/answers out, never authority-bearing mutations; paid services get no broader data access than free local models.
  - `haven/extensions/intelligence.py`: `IntelligenceBoundary` builds the `BoundedContext` (visible scopes only, capped 25/25, titles+propositions only — locators and out-of-scope records never cross, tested); responses are validated `IntelligenceResponse`s (answer / action/mutation proposal / clarification, reusing the existing intent types) returned as an envelope the caller routes through the governed paths — the boundary never touches a store. **Enforcement tested**: a malicious service's "delete everything" proposal leaves every store untouched, and routing that same proposal through `director._run_proposal` makes the guarded action PEND (authority engaged, never silent); invalid/wrong-type responses are refused. `EchoIntelligenceService` (diagnostic, builtin) proves the seam; `intelligence.echo` IPC returns the answer plus the exact bounded context the service saw.
- [~] Feature-module contract: native/domain extensions must call application services, not stores directly.
  - Contract shape only: the taxonomy declares the boundary (application services, never stores) and the native surface shows an honest empty state; no module loader exists yet — first-party features already demonstrate the services-only idiom.

# Definition of done (spec page 44, verbatim target)

HAVEN is "native life assistant v1" when a fresh user can install it, create projects/tasks/people, point it at allowed files, search and correlate their information, inspect/correct memory, use local or external models, perform governed computer/home actions, and close/reopen the native app without touching a browser surface — while every action remains scoped, explainable, and independently verifiable where the provider permits it.

# Repo baseline survey (branch main @ 83672f6)

Facts below ground the milestone notes above; revisit this section when a milestone starts.

- **Identity/scopes (C).** Identity is `Principal(actor_id, household_id, role_tier)` — there is no `principal_id` concept yet (`haven/core/domain.py:198`). `haven/identity/contracts.py` has the `IdentityProvider` Protocol + `ScopeMembership` but explicitly no local implementation; `haven/scopes/models.py` has `ScopeRef(scope_id, kind, name, parent_scope_id)` — contract only, no registry/persistence. `household_id` is the de-facto isolation scope, hard-rejected otherwise at `haven/web/server.py:265-278` (`_visible_scope_ids` allows only the authenticated household) and again at server.py:702-731; it appears 323× across 28 files. Resources/claims/ontology are already keyed by generic `scope_id` — the plumbing upgrade is real but bounded. People exist only as setup declarations, not records.
- **Domains (D).** No `haven/domains` or `haven/application` package; no Project/Task/Person records. Ontology predicates (`assigned_to`, `depends_on`, … `haven/ontology/predicates.py:9-10`) and the `ScopeRef` docstring anticipate them.
- **Computer (E).** `haven/integrations/computer/filesystem.py` covers allowed-roots scanning, symlink re-checks, read-only mode, and a governed action set (`filesystem.open/reveal/create_folder/copy/move/rename` via the risk table in `haven/web/computer_actions.py:68-76` and `ResourceAuthorityEngine`). **No application/window awareness, no browser provider.** Extraction is plain-text only: `TEXT_SUFFIXES = {.txt, .md, .markdown, .rst}` (`haven/knowledge/extraction/content.py:11-15`) — the DOCX/PDF/HTML/structured/CSV extractors from spec page 22 are all greenfield, and content reads deliberately go through the provider boundary.
- **Comms (F).** Nothing exists — no calendar/email/conversation providers or contracts; only forward references in comments (`haven/actions/models.py:9`). Greenfield.
- **Models (B5).** `haven/models/` is substantial and working: full ModelManager lifecycle (search/inspect/install/register/assign/load/unload, `manager.py:195-610`), hash-verified downloads, persisted job store with SSE, web API at `haven/web/models_api.py`. Native parity is mostly view + IPC surface work, not new core.
- **Ontology (G).** `OntologyStore` (SQLite, indexed `edges_from`/`edges_to`) exists but is **never fed** — no relationship generation in production; search's ontology expansion (`haven/search/service.py:167-170`) would consume edges if any existed.
- **Sync (H).** `haven/sync/contracts.py` (`SyncRecord`/`SyncBatch`/`SyncProvider`) only; no implementation, no references elsewhere.
- **Plugins (I).** `haven/plugins/` is already the narrow receipt-consumer boundary (plugins never run inside HAVEN, never get live state — `docs/plugin-boundary.md`). A separate provider-plugin mechanism exists (`haven/providers/plugin.py`, `ProviderManifest` Protocol, loaded via `setup.providers.install`). Milestone I is taxonomy + surfaces, not new isolation machinery.
- **IPC (affects B–G).** The named-pipe dispatcher (`haven/ipc/dispatcher.py`) is strictly request/response — **no event push**. The spec's recommended events (`state.changed`, `project.changed`, `action.progress`, … page 16) need an event channel added; web SSE exists only browser-side (`/events`, `/api/models/events`) with naive `retry: 3000` reconnect, no resume cursor. B2–B6 views will need polling or a new IPC event stream.
- **Tests.** 125 files / 1258 tests, green at HEAD. The earlier `test_claim_admission.py`/`test_plugins_*` failures were environmental (broken ACLs on a stale pytest temp dir), not code. `tests/test_web_plugins.py` is flaky in full-suite runs (live-network catalog tests, `WinError 10053` while booting a real server); each failure passes in isolation.
